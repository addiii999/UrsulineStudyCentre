import sys

path = r'c:\Users\adity\Documents\GitHub Projects\UrsulineStudyCentre\src\components\admin\AdminStorageManager.tsx'
with open(path, 'r', encoding='utf-8') as f:
    content = f.read()

old = '      )}\n\n      {tab === "backup"'
new = '''      )}\n\n      {tab === "queue" && !loading && (\n        <div className="space-y-4">\n          <div className="flex items-center justify-between bg-amber-50 border border-amber-200 rounded-2xl px-5 py-4">\n            <div>\n              <p className="font-bold text-amber-800 text-[13px]">Scheduled Permanent Deletion</p>\n              <p className="text-[11px] text-amber-600 mt-0.5">Records purge after 30 days. Restore within the window to cancel.</p>\n            </div>\n            <button onClick={handleRunPurge} disabled={busy === "purge"} className="flex items-center gap-2 px-4 py-2 rounded-xl bg-rose-600 text-white text-[11px] font-bold hover:bg-rose-700 disabled:opacity-50 transition-colors">\n              {busy === "purge" ? <Loader2 size={11} className="animate-spin" /> : <Trash2 size={11} />} Run Auto-Purge\n            </button>\n          </div>\n          <div className="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden">\n            {queue.length === 0 ? (\n              <div className="flex flex-col items-center justify-center py-16 gap-3">\n                <CheckCircle2 size={36} className="text-gray-200" />\n                <p className="font-semibold text-gray-400">No records scheduled for deletion</p>\n                <p className="text-[12px] text-gray-300">When records are scheduled from Student Trash, they appear here.</p>\n              </div>\n            ) : (\n              <div className="divide-y divide-gray-50">\n                {queue.map(item => {\n                  const daysLeft = Math.max(0, Math.ceil((new Date(item.scheduled_deletion_at).getTime() - Date.now()) / 86400000));\n                  const urgent = daysLeft <= 7;\n                  return (\n                    <div key={item.id} className={`flex items-center gap-4 px-5 py-4 transition-colors ${urgent ? "bg-rose-50/30" : "hover:bg-gray-50/60"}`}>\n                      <div className="flex-1 min-w-0">\n                        <div className="flex items-center gap-2 mb-1">\n                          <span className="text-[11px] font-bold text-[#800000] bg-[#800000]/10 px-2 py-0.5 rounded-full">Student</span>\n                          {urgent && <span className="text-[10px] font-bold text-rose-600 bg-rose-50 border border-rose-200 px-2 py-0.5 rounded-full flex items-center gap-1"><AlertTriangle size={9} />Purging soon</span>}\n                        </div>\n                        <p className="text-[13px] font-bold text-gray-800">{item.student_name}</p>\n                        <p className="text-[10px] text-gray-400 mt-0.5">{item.student_phone} &middot; {item.student_class}</p>\n                        <div className="flex flex-wrap items-center gap-4 mt-1.5">\n                          <span className="text-[10px] text-gray-400">Requested: <strong>{new Date(item.deletion_requested_at).toLocaleDateString("en-IN")}</strong></span>\n                          <span className={`text-[10px] font-bold ${urgent ? "text-rose-600" : "text-amber-600"}`}>Purges in {daysLeft} day(s) &bull; {new Date(item.scheduled_deletion_at).toLocaleDateString("en-IN")}</span>\n                          <span className="text-[10px] text-gray-400">Deleted by: {item.deleted_by}</span>\n                        </div>\n                      </div>\n                      <button onClick={() => handleRestoreFromQueue(item)} disabled={busy === item.id}\n                        className="flex items-center gap-1.5 px-3 py-1.5 rounded-xl bg-emerald-50 text-emerald-700 text-[11px] font-bold hover:bg-emerald-100 disabled:opacity-50 transition-colors whitespace-nowrap">\n                        {busy === item.id ? <Loader2 size={11} className="animate-spin" /> : <RotateCcw size={11} />} Restore\n                      </button>\n                    </div>\n                  );\n                })}\n              </div>\n            )}\n          </div>\n        </div>\n      )}\n\n      {tab === "backup"'''

if old not in content:
    print('MARKER NOT FOUND')
    sys.exit(1)

content = content.replace(old, new, 1)
with open(path, 'w', encoding='utf-8') as f:
    f.write(content)

print('DONE')
